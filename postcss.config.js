module.exports = {
    plugins: {
        autoprefixer: {
            browserlist: [
                "last 3 versions",
                "not < 0.5%",
                "not dead",
                "Firefox ESR",
            ]
        }
    },
}
